# LENCHI Wiki — エージェント向け運用スキーマ

このリポジトリは書籍プロジェクト **「LLMを使いこなすエンジニアの知的生産術」** （著者: 西尾泰和、2024年〜進行中・未完）を、未来のLLMエージェントが「記憶」として保持し、知的生産を継続できるようにするためのWikiである。`llm-wiki.md` のパターンを踏襲する。

## 3層アーキテクチャ

| 層 | パス | 役割 | 編集権限 |
|---|---|---|---|
| Raw（一次資料） | `raw/` | Scrapbox（Cosense）からエクスポートした原文。改変禁止 | LLMは読むのみ |
| Wiki（合成知） | `wiki/` | LLMが生成・整備する Markdown 群。エンティティ／概念／章ドラフト／タイムライン／未解決の問い | LLMが所有 |
| Schema（本ファイル） | `CLAUDE.md` | 構造・規約・運用フロー。利用者と共に進化させる | 利用者と共同 |

## ディレクトリレイアウト

```
lenchi/
├── llm-wiki.md          ← Wiki構築の元になった発想（Andrej Karpathy）
├── CLAUDE.md            ← 本ファイル（スキーマ）
├── raw/
│   └── nishio-llmを使いこなすエンジニアの知的生産術.2hop.txt
└── wiki/
    ├── index.md          ← 全ページのカタログ（コンテンツ志向）
    ├── log.md            ← Ingest/Query/Lint の時系列ログ
    ├── overview.md       ← プロジェクト全体の合成（最初に読むページ）
    ├── concepts/         ← 概念ページ（異知性コミュニケーション 等）
    ├── entities/         ← エンティティページ（人物・書籍・モデル・ツール）
    ├── chapters/         ← 章ドラフトの要約と現状
    ├── timeline/         ← Day1〜Day10 の協働プロセス記録
    └── questions/        ← 未解決の問い・継続課題
```

## ページの命名と相互リンク

- すべて Markdown。日本語ファイル名を許容する。
- ページ間リンクは相対パス + Markdown link（例: `[異知性コミュニケーション](../concepts/異知性コミュニケーション.md)`）。
- 概念・エンティティへの初出参照は必ずリンク化する。読者（人間 or LLM）がリンクを辿るだけで全体像にアクセスできる状態を保つ。
- 概念ページの冒頭は **1〜2行の定義** から始める。詳細は後段。

## ページ・テンプレート

### concepts/*.md

```markdown
# {{概念名}}

{{1〜2行の定義}}

## 来歴
- いつ・どの Day の対話で言語化されたか
- 西尾の問題意識との接続

## 含意 / 議論
- 著者がこの概念をどう使っているか
- 関連する他の概念へのリンク

## 未解決の問い
- この概念について残されている問題（あれば [questions/未解決の問い.md] を更新）

## 出典
- raw のページ名と、そのページが扱っている内容
```

### entities/*.md

```markdown
# {{固有名}}

{{1行の説明}}

## 役割（プロジェクトでの）
## 関連概念 / イベント
## 出典
```

### chapters/*.md

```markdown
# {{章名}}

- 完成度: XX%
- 生成日 / 生成者: 2024-mm-dd / Claude 3 Opus 等
- 次にやるべきこと:

## 章の要旨
## 主要な主張
## 含まれる本文（要約 / 完全版へのポインタ）
## 参照している概念・エンティティ
```

### timeline/Day*.md

```markdown
# Day {{N}} — {{日付}}

## 何が起きたか（1段落）
## この日に言語化された概念（リンク）
## この日のキー対話 / 引用
## 引き継ぎ事項（次の Day へ）
```

### questions/*.md

```markdown
# {{タイトル}}

{{1〜2行の趣旨}}

## {{グループ見出し}}（理論的問い／編集上の問い／運用上の作業課題 など）

### Q{{N}}. {{問いのコア}}（必要なら状態タグ：**半解決** / **優先度高** など）
{{背景・関連リンク・次にやること}}
```

問いは **解けたら削るのではなく、解の状況を更新する**（半解決・解決済みも含めて履歴を残す）。

## 運用フロー

### Ingest（一次資料を取り込む）

利用者が新しい資料（Scrapbox エクスポート、新しい Day の対話、講演スライドなど）を `raw/` に追加して指示したら：

1. 資料を読み、利用者と要点を確認する。
2. 影響を受けるページを `wiki/index.md` から特定する。
3. 関連する `concepts/`、`entities/`、`chapters/`、`timeline/` を更新する。**新しい主張が既存の主張と矛盾する場合は両方を残し、矛盾を明示する**（消さない）。
4. 必要なら新しいページを作り、`index.md` に登録する。
5. `log.md` に `## [YYYY-MM-DD] ingest | 資料名` で1エントリ追記する。

### Query（Wikiに問いかける）

利用者が問いを投げたら：

1. `index.md` でまず関連ページを特定する。
2. 該当ページを読み、必要に応じて `raw/` の該当箇所まで遡る。
3. 答えにオリジナリティがある場合（比較表・新しい統合・問いのリスト等）、**その答え自体を新しい Wiki ページとして保存する**ことを検討する。質問の蓄積も知的資産。
4. `log.md` に `## [YYYY-MM-DD] query | 質問の要約` を追記。

### Lint（健康診断）

定期的に、または大きな ingest の後で：

- 概念ページのうち、他から1度もリンクされていないもの（孤児）はないか。
- 矛盾する記述、古くなった主張（新しいモデル登場で陳腐化していないか）はないか。
- 重要な概念だが独立ページが無いものはないか。
- `chapters/` に書かれた「次にやるべきこと」は最新か。
- `questions/未解決の問い.md` は更新されているか。

## 本プロジェクトに固有の方針

- **本書の執筆プロセス自体が本書の素材である**。Day1〜Day10 のプロセスログは「事例」として本文に組み込まれる予定なので、`timeline/` を大事に扱う。
- 著者は「LLMの出力を鵜呑みにせず批判的に吟味する」姿勢を取っている。Wikiの記述も同じ姿勢を反映する：LLMが生成した断片は出典を残し、後から追跡可能にする。
- 著者の問題意識：**知的生産の本質的部分（KJ法、学びのサイクル、ニーズ駆動）はLLM時代でも有効か、どう進化するのか**。新しい資料を取り込むときは必ずこの軸で整理する。
- 章ドラフト（`chapters/前書き.md`、`chapters/第一章.md`、`chapters/第二章.md`）は出版候補レベル。安易に書き換えず、変更は履歴を残して提案する。
- 未解決の問い（`questions/未解決の問い.md`）は、次のセッションで取り組むべきタスクの源泉。常に最新化する。

## 言語

- すべて日本語で書く（原資料が日本語のため）。
- 引用や用語は原文の表記を尊重する（例: 「Scrapbox（現Cosense）」「Kozaneba」）。
